export function randomNumbers() {
    return Math.random();
}

export function findMaxFromArray(array: any) {
    return Math.max(...array);
}

export function findMinFromArray(array: any) {
    return Math.min(...array);
}